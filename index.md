# Serenayin.github.io


# GitHub Actions
## Demo of REACT APP

action of checkout will make a copy of project in the root(which will make the path to something like /home/user/repo/repo/...

### sub-project key point

use working-directory to assign shell the directory to run in; specify relative path in folder of Build or things like that.
